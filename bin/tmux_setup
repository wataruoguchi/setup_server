#!/bin/bash
set -x 
set -e

# Specify the config file
# TODO give a better path
TMUX="tmux -f /home/vagrant/.tmux.conf"

# Start a new session
$TMUX new-session

# Attach to the session
$TMUX attach-session
